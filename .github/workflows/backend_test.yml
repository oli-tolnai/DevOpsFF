name: Deploy Backend to Test

on:
  push:
    branches: [ "development" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      # --- ÁLLÍTHATÓ ---
      PROJECT_PATH: BACKEND/IssueTracker/IssueTracker.Endpoint.csproj
      PUBLISH_DIR: publish
      RUNTIME: linux-x64
      SELF_CONTAINED: "true"
      PUBLISH_SINGLE_FILE: "true"
      # TEST könyvtárak (Debian szerveren)
      REMOTE_BASE: /var/backend_test
      REMOTE_RELEASES: /var/backend_test/releases
      REMOTE_CURRENT: /var/backend_test/current
      SERVICE_NAME: issuetrackerbackend_test
      FRONTEND_URL: https://test.issuetracker.hu
      SSH_HOST: test.issuetracker.hu
      SSH_USER: root
      # --- SECRET-ek ---
      # SSH_KEY, SSH_PASS, DB_CONN_TEST (új!)
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      
      - name: Cache NuGet
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-
      
      - name: Restore
        run: dotnet restore ${{ env.PROJECT_PATH }}
        
      - name: Publish (Release, Linux)
        run: |
          dotnet publish ${{ env.PROJECT_PATH }} \
            -c Release \
            -r ${{ env.RUNTIME }} \
            -p:PublishSingleFile=${{ env.PUBLISH_SINGLE_FILE }} \
            --self-contained ${{ env.SELF_CONTAINED }} \
            -o ${{ env.PUBLISH_DIR }}
            
      - name: Pack artifact (tar.gz)
        run: |
          tar -C ${{ env.PUBLISH_DIR }} -czf app-${{ github.sha }}.tar.gz . 
      
      - name: Add remote host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ env.SSH_HOST }}" >> ~/.ssh/known_hosts
          
      - name: Upload package via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASS }}
          source: "app-${{ github.sha }}.tar.gz"
          target: "/tmp/"
      
      - name: Deploy on server (atomic symlink switch + systemd restart)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ env.SSH_HOST }}
          username: ${{ env.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          passphrase: ${{ secrets.SSH_PASS }}
          script: |
            set -euo pipefail
            
            SERVICE="${{ env.SERVICE_NAME }}"
            BASE="${{ env.REMOTE_BASE }}"
            RELEASES="${{ env.REMOTE_RELEASES }}"
            CURRENT="${{ env.REMOTE_CURRENT }}"
            TS="$(date +%Y%m%d%H%M%S)-${{ github.sha }}"
            NEW_RELEASE="${RELEASES}/${TS}"
            
            sudo mkdir -p "$RELEASES"
            sudo mkdir -p "$BASE"
            sudo systemctl stop "$SERVICE" || true
            
            # Kibontás új release mappába
            sudo mkdir -p "$NEW_RELEASE"
            sudo tar -xzf "/tmp/app-${{ github.sha }}.tar.gz" -C "$NEW_RELEASE"
            sudo rm -f "/tmp/app-${{ github.sha }}.tar.gz"
            
            # appsettings.json módosítása (DB_CONN_TEST + test frontend URL)
            if command -v jq >/dev/null; then
              sudo jq \
                --arg conn "${{ secrets.DB_CONN_TEST }}" \
                --arg frontend "${{ env.FRONTEND_URL }}" \
                '.db.conn = $conn | .settings.frontend = $frontend' \
                "$NEW_RELEASE/appsettings.json" | sudo tee "$NEW_RELEASE/appsettings.json.tmp" > /dev/null
              sudo mv "$NEW_RELEASE/appsettings.json.tmp" "$NEW_RELEASE/appsettings.json"
            else
              echo "jq nincs telepítve, telepítem..."
              sudo apt-get update && sudo apt-get install -y jq
              sudo jq \
                --arg conn "${{ secrets.DB_CONN_TEST }}" \
                --arg frontend "${{ env.FRONTEND_URL }}" \
                '.db.conn = $conn | .settings.frontend = $frontend' \
                "$NEW_RELEASE/appsettings.json" | sudo tee "$NEW_RELEASE/appsettings.json.tmp" > /dev/null
              sudo mv "$NEW_RELEASE/appsettings.json.tmp" "$NEW_RELEASE/appsettings.json"
            fi
            
            # Symlink csere atomikusan
            # sudo ln -sfn "$NEW_RELEASE" "$CURRENT" EZ ROSSZ?
            sudo rm -f "$BASE/current"
            sudo ln -s "$NEW_RELEASE" "$BASE/current"

            
            # Service újraindítás + engedélyezés
            sudo systemctl daemon-reload || true
            sudo systemctl start "$SERVICE"
            sudo systemctl enable "$SERVICE" || true
            
            # Régi release-ek törlése (tartsunk meg 5-öt)
            COUNT=$(ls -1dt "$RELEASES"/* 2>/dev/null | wc -l || true)
            if [ "$COUNT" -gt 5 ]; then
              ls -1dt "$RELEASES"/* | tail -n +6 | xargs -r sudo rm -rf
            fi
            
            sudo systemctl status "$SERVICE" --no-pager -l || true
